<Window x:Class="QuanLyThuVien.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QuanLyThuVien.Views"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Title="Đăng nhập hệ thống" Height="500" Width="400"
        Background="{DynamicResource Brush.WindowBackground}">
    <Grid>
        <Border Style="{StaticResource CardStyle}" VerticalAlignment="Center" HorizontalAlignment="Center" Width="320" Padding="24">
            <StackPanel>
                <TextBlock Text="THƯ VIỆN" Style="{StaticResource TitleTextStyle}" HorizontalAlignment="Center" FontSize="28"/>
                <TextBlock Text="Đăng nhập hệ thống quản lý" Style="{StaticResource CaptionTextStyle}" HorizontalAlignment="Center" Margin="0,5,0,20"/>

                <TextBlock Text="Tên đăng nhập" Style="{StaticResource LabelTextStyle}"/>
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource AppTextBoxStyle}"/>

                <TextBlock Text="Mật khẩu" Style="{StaticResource LabelTextStyle}" Margin="0,10,0,0"/>
                <PasswordBox x:Name="txtPassword" Style="{StaticResource AppPasswordBoxStyle}"/>

                <Button Content="ĐĂNG NHẬP" Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=txtPassword}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,20,0,0" Height="45"/>
                <Button Content="THOÁT" Style="{StaticResource DangerButtonStyle}" Height="45" IsCancel="True" Command="{Binding ExitCommand}" Margin="0 10 0 0"></Button>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="{DynamicResource Brush.Danger}" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,10,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
