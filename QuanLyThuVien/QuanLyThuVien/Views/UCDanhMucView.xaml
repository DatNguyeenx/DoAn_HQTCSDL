<UserControl x:Class="QuanLyThuVien.Views.UCDanhMucView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuanLyThuVien.Views"
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="1100">
    <UserControl.Resources>
        <Style TargetType="TabItem">
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontSize" Value="{StaticResource Font.Section}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{DynamicResource Brush.TextSecondary}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0,0,0,3"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Margin" Value="0,0,20,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border Name="Border" Padding="{TemplateBinding Padding}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                Background="{TemplateBinding Background}">
                            <ContentPresenter x:Name="ContentSite"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Center"
                                              ContentSource="Header" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Foreground" Value="{DynamicResource Brush.PrimaryDark}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource Brush.PrimaryDark}"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="{DynamicResource Brush.PrimaryDark}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <TabControl Background="Transparent" BorderThickness="0">

            <TabItem  Width="200" Header="THỂ LOẠI SÁCH">
                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="360"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource CardStyle}" Padding="0" Margin="0,0,20,0">
                        <DataGrid ItemsSource="{Binding ListLoaiSach}" SelectedItem="{Binding SelectedLoaiSach}" Style="{StaticResource AppDataGridStyle}" Margin="1">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Mã Thể Loại" Binding="{Binding MaLoai}" Width="150"/>
                                <DataGridTextColumn Header="Tên Thể Loại" Binding="{Binding TenLoai}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>

                    <Border Grid.Column="1" Style="{StaticResource CardStyle}" VerticalAlignment="Top">
                        <StackPanel>
                            <TextBlock Text="THÔNG TIN THỂ LOẠI" Style="{StaticResource SectionTitleTextStyle}" Margin="0,0,0,24"/>

                            <TextBlock Text="Mã thể loại" Style="{StaticResource LabelTextStyle}"/>
                            <TextBox Text="{Binding MaLoai, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource AppTextBoxStyle}" IsEnabled="{Binding IsEditingLoaiMode}"/>

                            <TextBlock Text="Tên thể loại" Style="{StaticResource LabelTextStyle}"/>
                            <TextBox Text="{Binding TenLoai, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource AppTextBoxStyle}"/>

                            <Separator Margin="0,16,0,24" Background="{DynamicResource Brush.BorderSoft}"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Button Grid.Row="0" Grid.Column="0" Content="✚ THÊM MỚI" Command="{Binding AddCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="0 0 10 0"/>

                                <Button Grid.Row="0" Grid.Column="1" Content="✎ SỬA" Command="{Binding UpdateCommand}" Style="{StaticResource SuccessButtonStyle}" Margin="0 0 10 0"/>

                                <Button Grid.Row="1" Grid.Column="0" Content="✖ XÓA" Command="{Binding DeleteCommand}" Style="{StaticResource DangerButtonStyle}" Margin="0 10 10 0"/>

                                <Button Grid.Row="1" Grid.Column="1" Content="↻ LÀM MỚI" Command="{Binding ClearCommand}" Style="{StaticResource BaseButtonStyle}" Margin="0 10 10 0"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>

            <TabItem  Width="200" Header="NHÀ XUẤT BẢN">
                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="360"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource CardStyle}" Padding="0" Margin="0,0,20,0">
                        <DataGrid ItemsSource="{Binding ListNXB}" SelectedItem="{Binding SelectedNXB}" Style="{StaticResource AppDataGridStyle}" Margin="1">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Mã NXB" Binding="{Binding MaNXB}" Width="100"/>
                                <DataGridTextColumn Header="Tên NXB" Binding="{Binding TenNXB}" Width="*"/>
                                <DataGridTextColumn Header="Địa Chỉ" Binding="{Binding DiaChi}" Width="160"/>
                                <DataGridTextColumn Header="Số ĐT" Binding="{Binding SoDT}" Width="130"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>

                    <Border Grid.Column="1" Style="{StaticResource CardStyle}" VerticalAlignment="Top">
                        <StackPanel>
                            <TextBlock Text="THÔNG TIN NHÀ XUẤT BẢN" Style="{StaticResource SectionTitleTextStyle}" Margin="0,0,0,24"/>

                            <TextBlock Text="Mã NXB" Style="{StaticResource LabelTextStyle}"/>
                            <TextBox Text="{Binding MaNXB, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource AppTextBoxStyle}" IsEnabled="{Binding IsEditingNXBMode}"/>

                            <TextBlock Text="Tên NXB" Style="{StaticResource LabelTextStyle}"/>
                            <TextBox Text="{Binding TenNXB, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource AppTextBoxStyle}"/>

                            <TextBlock Text="Địa chỉ" Style="{StaticResource LabelTextStyle}"/>
                            <TextBox Text="{Binding DiaChiNXB, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource AppTextBoxStyle}"/>

                            <TextBlock Text="Số điện thoại" Style="{StaticResource LabelTextStyle}"/>
                            <TextBox Text="{Binding SoDTNXB, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource AppTextBoxStyle}"/>

                            <Separator Margin="0,16,0,24" Background="{DynamicResource Brush.BorderSoft}"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Button Grid.Row="0" Grid.Column="0" Content="✚ THÊM MỚI" Command="{Binding AddCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="0 0 10 0"/>

                                <Button Grid.Row="0" Grid.Column="1" Content="✎ SỬA" Command="{Binding UpdateCommand}" Style="{StaticResource SuccessButtonStyle}" Margin="0 0 10 0"/>

                                <Button Grid.Row="1" Grid.Column="0" Content="✖ XÓA" Command="{Binding DeleteCommand}" Style="{StaticResource DangerButtonStyle}" Margin="0 10 10 0" />

                                <Button Grid.Row="1" Grid.Column="1" Content="↻ LÀM MỚI" Command="{Binding ClearCommand}" Style="{StaticResource BaseButtonStyle}" Margin="0 10 10 0"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>

            <TabItem  Width="200" Header="TÁC GIẢ">
                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="360"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource CardStyle}" Padding="0" Margin="0,0,20,0">
                        <DataGrid ItemsSource="{Binding ListTacGia}" SelectedItem="{Binding SelectedTacGia}" Style="{StaticResource AppDataGridStyle}" Margin="1">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Mã Tác Giả" Binding="{Binding MaTG}" Width="150"/>
                                <DataGridTextColumn Header="Tên Tác Giả" Binding="{Binding TenTG}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>

                    <Border Grid.Column="1" Style="{StaticResource CardStyle}" VerticalAlignment="Top">
                        <StackPanel>
                            <TextBlock Text="THÔNG TIN TÁC GIẢ" Style="{StaticResource SectionTitleTextStyle}" Margin="0,0,0,24"/>

                            <TextBlock Text="Mã tác giả" Style="{StaticResource LabelTextStyle}"/>
                            <TextBox Text="{Binding MaTG, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource AppTextBoxStyle}" IsEnabled="{Binding IsEditingTacGiaMode}"/>

                            <TextBlock Text="Tên tác giả" Style="{StaticResource LabelTextStyle}"/>
                            <TextBox Text="{Binding TenTG, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource AppTextBoxStyle}"/>

                            <Separator Margin="0,16,0,24" Background="{DynamicResource Brush.BorderSoft}"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Button Grid.Row="0" Grid.Column="0" Content="✚ THÊM MỚI" Command="{Binding AddCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="0 0 10 0"/>

                                <Button Grid.Row="0" Grid.Column="1" Content="✎ SỬA" Command="{Binding UpdateCommand}" Style="{StaticResource SuccessButtonStyle}" Margin="0 0 10 0"/>

                                <Button Grid.Row="1" Grid.Column="0" Content="✖ XÓA" Command="{Binding DeleteCommand}" Style="{StaticResource DangerButtonStyle}" Margin="0 10 10 0" />

                                <Button Grid.Row="1" Grid.Column="1" Content="↻ LÀM MỚI" Command="{Binding ClearCommand}" Style="{StaticResource BaseButtonStyle}" Margin="0 10 10 0"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>

        </TabControl>
    </Grid>
</UserControl>
