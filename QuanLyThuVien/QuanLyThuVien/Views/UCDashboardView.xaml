<UserControl x:Class="QuanLyThuVien.Views.UCDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuanLyThuVien.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Style="{StaticResource HighlightCardStyle}">
            <StackPanel>
                <TextBlock Text="Xin chào, thủ thư!" Style="{StaticResource TitleTextStyle}"/>
                <TextBlock Text="Hệ thống quản lý thư viện hiện đại, trực quan và dễ sử dụng." Style="{StaticResource BodyTextStyle}" Margin="0,8,0,0"/>
            </StackPanel>
        </Border>

        <!-- Các thẻ thống kê nhanh -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardStyle}" Background="{DynamicResource Brush.Primary}">
                <StackPanel>
                    <TextBlock Text="Tổng sách trong kho" Style="{StaticResource CaptionTextStyle}" Foreground="White"/>
                    <TextBlock Text="{Binding TongSach}" Style="{StaticResource SectionTitleTextStyle}" Foreground="White" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardStyle}" Background="{DynamicResource Brush.Warning}">
                <StackPanel>
                    <TextBlock Text="Sách đang được mượn" Style="{StaticResource CaptionTextStyle}"/>
                    <TextBlock Text="{Binding SachDangMuon}" Style="{StaticResource SectionTitleTextStyle}" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="4" Style="{StaticResource CardStyle}" Background="{DynamicResource Brush.Success}">
                <StackPanel>
                    <TextBlock Text="Tổng số độc giả" Style="{StaticResource CaptionTextStyle}"/>
                    <TextBlock Text="{Binding TongDocGia}" Style="{StaticResource SectionTitleTextStyle}" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="6" Style="{StaticResource CardStyle}" Background="{DynamicResource Brush.DangerSoft}">
                <StackPanel>
                    <TextBlock Text="Tiền phạt thu được (VNĐ)" Style="{StaticResource CaptionTextStyle}"/>
                    <TextBlock Text="{Binding TienPhatThuDuoc}" Style="{StaticResource SectionTitleTextStyle}" Foreground="{DynamicResource Brush.Danger}" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- DataGrid sách quá hạn (Placeholder) -->
        <Border Grid.Row="4" Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Độc giả đang nợ sách (Quá hạn)" Style="{StaticResource SectionTitleTextStyle}" Margin="0,0,0,10"/>
                <DataGrid Grid.Row="1" ItemsSource="{Binding ListQuahan}" Style="{StaticResource AppDataGridStyle}">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                            <Setter Property="HorizontalContentAlignment" Value="Center" />
                        </Style>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type DataGridCell}">
                                        <Grid Background="{TemplateBinding Background}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                        </Grid>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Độc giả" Binding="{Binding HoTen}" Width="*"/>
                        <DataGridTextColumn Header="Sách mượn" Binding="{Binding TenSach}" Width="*"/>
                        <DataGridTextColumn Header="Hạn trả" Binding="{Binding HanTra, StringFormat=dd/MM/yyyy}" Width="120"/>
                        <DataGridTextColumn Header="Trạng thái" Binding="{Binding TrangThaiTra}" Width="150"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
